package dev.jakub.discord.translation.service.google;

public class GoogleTranslationServiceProvider extends GoogleTranslationService {

    public GoogleTranslationServiceProvider(String authKey) {
        super(authKey);
    }
}